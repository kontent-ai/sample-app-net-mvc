@model VisualContainerViewModel

@{
    var carouselId = $"stack-carousel-{Guid.NewGuid():N}";
    var hasMultipleItems = Model.Items.Count > 1;
}

<section class="visual-container visual-container--stack py-5" data-kontent-item-id="@Model.ItemId">
    <div class="container">
        @if (!string.IsNullOrEmpty(Model.Title))
        {
            <h2 class="mb-3" data-kontent-element-codename="title">@Model.Title</h2>
        }
        @if (!string.IsNullOrEmpty(Model.Subtitle))
        {
            <p class="text-muted mb-4" data-kontent-element-codename="subtitle">@Model.Subtitle</p>
        }

        @if (hasMultipleItems)
        {
            <div id="@carouselId" class="carousel slide stack-carousel" data-bs-ride="false" data-kontent-element-codename="items">
                <div class="carousel-inner">
                    @for (var i = 0; i < Model.Items.Count; i++)
                    {
                        var fact = Model.Items[i];
                        <div class="carousel-item @(i == 0 ? "active" : "")" data-kontent-item-id="@fact.ItemId">
                            <div class="row align-items-center">
                                <div class="col-md-6">
                                    @if (fact.Image != null)
                                    {
                                        <img src="@fact.Image.UrlWithTransform(width: 800, format: ImageFormat.Webp, quality: 80)" class="img-fluid rounded" alt="@(fact.Image.AltText ?? fact.Title ?? "")" data-kontent-element-codename="image" />
                                    }
                                </div>
                                <div class="col-md-6">
                                    <div class="stack-content p-4">
                                        @if (!string.IsNullOrEmpty(fact.Title))
                                        {
                                            <h3 class="stack-title" data-kontent-element-codename="title">@fact.Title</h3>
                                        }
                                        @if (!string.IsNullOrEmpty(fact.Message))
                                        {
                                            <p class="stack-message" data-kontent-element-codename="message">@fact.Message</p>
                                        }
                                        @if (!string.IsNullOrWhiteSpace(fact.LinkLabel))
                                        {
                                            @await Html.PartialAsync("_FactReference", fact)
                                        }
                                    </div>
                                </div>
                            </div>
                        </div>
                    }
                </div>
                <button class="carousel-control-prev" type="button" data-bs-target="#@carouselId" data-bs-slide="prev">
                    <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                    <span class="visually-hidden">Previous</span>
                </button>
                <button class="carousel-control-next" type="button" data-bs-target="#@carouselId" data-bs-slide="next">
                    <span class="carousel-control-next-icon" aria-hidden="true"></span>
                    <span class="visually-hidden">Next</span>
                </button>
                <div class="carousel-indicators position-relative mt-3">
                    @for (var i = 0; i < Model.Items.Count; i++)
                    {
                        <button type="button"
                                data-bs-target="#@carouselId"
                                data-bs-slide-to="@i"
                                class="@(i == 0 ? "active" : "")"
                                aria-current="@(i == 0 ? "true" : "false")"
                                aria-label="Slide @(i + 1)"></button>
                    }
                </div>
            </div>
        }
        else if (Model.Items.Count == 1)
        {
            var fact = Model.Items[0];
            <div class="row align-items-center" data-kontent-item-id="@fact.ItemId">
                <div class="col-md-6">
                    @if (fact.Image != null)
                    {
                        <img src="@fact.Image.UrlWithTransform(width: 800, format: ImageFormat.Webp, quality: 80)" class="img-fluid rounded" alt="@(fact.Image.AltText ?? fact.Title ?? "")" data-kontent-element-codename="image" />
                    }
                </div>
                <div class="col-md-6">
                    <div class="stack-content p-4">
                        @if (!string.IsNullOrEmpty(fact.Title))
                        {
                            <h3 class="stack-title" data-kontent-element-codename="title">@fact.Title</h3>
                        }
                        @if (!string.IsNullOrEmpty(fact.Message))
                        {
                            <p class="stack-message" data-kontent-element-codename="message">@fact.Message</p>
                        }
                        @if (!string.IsNullOrWhiteSpace(fact.LinkLabel))
                        {
                            @await Html.PartialAsync("_FactReference", fact)
                        }
                    </div>
                </div>
            </div>
        }
    </div>
</section>
