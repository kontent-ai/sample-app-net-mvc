@model FactViewModel

@if (!string.IsNullOrWhiteSpace(Model.LinkLabel))
{
    <a href="@Model.LinkUrl"
       class="btn btn-primary"
       @(Model.LinkIsExternal ? "target=\"_blank\" rel=\"noopener noreferrer\"" : "")>
        @Model.LinkLabel
    </a>
}
