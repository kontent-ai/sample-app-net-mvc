@model WebsiteRootViewModel

@{
    ViewData["Title"] = Model.MetadataTitle ?? Model.Title ?? "Home";
}

@foreach (var block in Model.Content)
{
    @await Html.PartialAsync(block.PartialViewName, block)
}
